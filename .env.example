# VelocityVault x402 Configuration
# Copy this to .env and fill in your values
# NEVER commit .env to git

# === WALLET (receives USDC payments on Base) ===
WALLET_ADDRESS=0xYourWalletAddressHere

# === NETWORK ===
# "base" for mainnet, "base-sepolia" for testnet
X402_NETWORK=base-sepolia

# === COINBASE DEVELOPER PLATFORM ===
# Get keys at https://portal.cdp.coinbase.com/
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=

# === SERVER ===
PORT=3000
NODE_ENV=production

# === SECURITY ===
ADMIN_API_KEY=generate-a-strong-random-string-here
RATE_LIMIT_MAX=100
RATE_LIMIT_X402_MAX=200

# === STATIC SITE ===
STATIC_DIR=./
